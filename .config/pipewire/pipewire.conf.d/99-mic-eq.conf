context.modules = [
  {
    name = libpipewire-module-parametric-equalizer
    args = {
      equalizer.filepath   = "/home/fib/.config/pipewire/mic-eq.txt"
      equalizer.description = "Mic EQ"
      audio.channels       = 2
      audio.position       = [ FL FR ]
      capture.props = {
        node.name        = "miceq_in"
        node.description = "Mic EQ input"
      }
      playback.props = {
        node.name        = "miceq_out"
        node.description = "Mic EQ output"
      }
    }
  }
]
